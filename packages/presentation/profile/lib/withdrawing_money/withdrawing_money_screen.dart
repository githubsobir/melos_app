import 'package:flutter/material.dart';

class WithdrawingMoneyScreen extends StatelessWidget {
  const WithdrawingMoneyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Withdrawing Money"),
      ),
    );
  }
}
